import { connectDb } from "@/lib/db/mongoose";import { MrCallSchedule } from "@/models/Catalog";import { PageHeader } from "@/components/ui/page-header";import { DataList } from "@/components/ui/data-list";const days=["Sunday","Monday","Tuesday","Wednesday","Thursday","Friday","Saturday"];export default async function Schedules(){await connectDb();const raw=await MrCallSchedule.find().populate("doctor","name").populate("clinic","name").sort({weekday:1}).limit(100).lean();const rows=raw.map(x=>({...x,weekday:days[x.weekday],slots:x.slots?.map((s:{start:string;end:string})=>`${s.start}–${s.end}`).join(", ")})) as unknown as Array<Record<string,unknown>>;return <div className="space-y-6"><PageHeader title="MR call schedule" subtitle="Weekly doctor availability"/><DataList rows={rows} columns={[{key:"doctor",label:"Doctor"},{key:"clinic",label:"Clinic"},{key:"weekday",label:"Day"},{key:"slots",label:"Time slots"},{key:"appointmentRequired",label:"Appointment"}]}/></div>}
