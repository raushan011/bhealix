import bcrypt from "bcryptjs";import { z } from "zod";import { getSession } from "@/lib/auth/session";import { connectDb } from "@/lib/db/mongoose";import { User } from "@/models/User";import { fail,ok } from "@/lib/api";const schema=z.object({currentPassword:z.string().min(8),newPassword:z.string().min(10).regex(/[A-Z]/).regex(/[0-9]/)});export async function POST(req:Request){try{const session=await getSession();if(!session)return Response.json({error:"Unauthenticated"},{status:401});const value=schema.parse(await req.json());await connectDb();const user=await User.findById(session.userId).select("+passwordHash");if(!user||!await bcrypt.compare(value.currentPassword,user.passwordHash))return Response.json({error:"Current password is incorrect"},{status:400});user.passwordHash=await bcrypt.hash(value.newPassword,12);await user.save();return ok({changed:true})}catch(e){return fail(e)}}
