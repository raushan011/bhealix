import * as XLSX from "xlsx";import { connectDb } from "@/lib/db/mongoose";import { Doctor } from "@/models/Doctor";
export async function GET(){await connectDb();const doctors=await Doctor.find({status:{$ne:"Archived"}}).select("code name specialties clinicName city area phones priority stage status createdAt").sort({name:1}).limit(10000).lean();const rows=doctors.map(d=>({"Doctor Code":d.code,"Doctor Name":d.name,Specialty:d.specialties?.join(", "),"Clinic Name":d.clinicName,Area:d.area,City:d.city,"Mobile Number":d.phones?.[0]??"",Priority:d.priority,"Lead Status":d.stage,Status:d.status,"Last Updated":d.updatedAt}));const book=XLSX.utils.book_new();XLSX.utils.book_append_sheet(book,XLSX.utils.json_to_sheet(rows),"Doctors");const output=XLSX.write(book,{type:"buffer",bookType:"xlsx"}) as Buffer;return new Response(new Uint8Array(output),{headers:{"content-type":"application/vnd.openxmlformats-officedocument.spreadsheetml.sheet","content-disposition":"attachment; filename=bhealix-doctors.xlsx"}})}
