import { notFound } from "next/navigation";import { connectDb } from "@/lib/db/mongoose";import { Doctor } from "@/models/Doctor";import { Assignment,Visit,FollowUp,Order } from "@/models/CRM";import { PageHeader } from "@/components/ui/page-header";import { DataList } from "@/components/ui/data-list";
const config={today:{title:"Today",model:Assignment,columns:[{key:"doctor",label:"Doctor"},{key:"scheduledTime",label:"Time"},{key:"status",label:"Status"}]},doctors:{title:"My doctors",model:Doctor,columns:[{key:"name",label:"Doctor"},{key:"clinicName",label:"Clinic"},{key:"area",label:"Area"},{key:"priority",label:"Priority"}]},assignments:{title:"Assignments",model:Assignment,columns:[{key:"doctor",label:"Doctor"},{key:"date",label:"Date"},{key:"status",label:"Status"}]},visits:{title:"Visits",model:Visit,columns:[{key:"doctor",label:"Doctor"},{key:"status",label:"Status"},{key:"outcome",label:"Outcome"}]},"follow-ups":{title:"Follow-ups",model:FollowUp,columns:[{key:"doctor",label:"Doctor"},{key:"dueAt",label:"Due"},{key:"status",label:"Status"}]},orders:{title:"Orders",model:Order,columns:[{key:"doctor",label:"Doctor"},{key:"total",label:"Value"},{key:"status",label:"Status"}]}} as const;
export default async function EmployeeSection({params}:{params:Promise<{section:string}>}){const {section}=await params;if(["profile","performance","calendar","notifications"].includes(section))return <PageHeader title={section[0].toUpperCase()+section.slice(1)} subtitle="Your account and activity"/>;if(!(section in config))notFound();const c=config[section as keyof typeof config];await connectDb();let query=c.model.find().sort({createdAt:-1}).limit(30);if(section!=="doctors")query=query.populate({path:"doctor",select:"name clinicName"});const rows=await query.lean() as unknown as Array<Record<string,unknown>>;return <div className="space-y-5"><PageHeader title={c.title} subtitle={`${rows.length} records`}/><DataList rows={rows} columns={[...c.columns]}/></div>}
