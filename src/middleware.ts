import { NextRequest,NextResponse } from "next/server";
import { jwtVerify } from "jose/jwt/verify";
export async function middleware(req:NextRequest){const path=req.nextUrl.pathname;if(path==="/api/auth/login"||path==="/api/auth/forgot-password")return NextResponse.next();const api=path.startsWith("/api/");const token=req.cookies.get("bhealix_session")?.value;if(!token)return api?NextResponse.json({error:"Authentication required"},{status:401}):NextResponse.redirect(new URL(`/login?next=${encodeURIComponent(path)}`,req.url));try{const secret=process.env.AUTH_SECRET;if(!secret)throw new Error("AUTH_SECRET missing");const {payload}=await jwtVerify(token,new TextEncoder().encode(secret));const role=String(payload.role);if((path.startsWith("/admin")||path.startsWith("/api/google")||path.startsWith("/api/excel"))&&role!=="ADMIN"&&role!=="HR")return api?NextResponse.json({error:"Forbidden"},{status:403}):NextResponse.redirect(new URL("/employee",req.url));const response=NextResponse.next();response.headers.set("x-content-type-options","nosniff");response.headers.set("x-frame-options","DENY");response.headers.set("referrer-policy","strict-origin-when-cross-origin");return response}catch{return api?NextResponse.json({error:"Invalid session"},{status:401}):NextResponse.redirect(new URL("/login",req.url))}}
export const config={matcher:["/admin/:path*","/employee/:path*","/api/:path*"]};
