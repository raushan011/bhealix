import { describe,expect,it } from "vitest";import { normalizePhone,radiusSchema,timeSlotSchema } from "./domain";describe("domain validation",()=>{it("rejects radius over 100 km",()=>expect(radiusSchema.safeParse(101).success).toBe(false));it("validates MR slot order",()=>expect(timeSlotSchema.safeParse({start:"16:00",end:"14:00"}).success).toBe(false));it("normalizes formatted phone numbers",()=>expect(normalizePhone("+91 98765-43210")).toBe("+919876543210"))});
